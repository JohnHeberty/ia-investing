# docker/profiles/test.env
# Variáveis para perfil de teste (PostgreSQL + MinIO only)

DATABASE__URL=postgresql+asyncpg://app:app-local-only@localhost:5432/stock_intelligence
STORAGE__ENDPOINT=http://localhost:9000
STORAGE__ACCESS_KEY=minioadmin
STORAGE__SECRET_KEY=minio-local-only
STORAGE__BUCKET=raw-documents
TEMPORAL__ADDRESS=localhost:7233
AI__PROVIDER=mock
APPLICATION__ENVIRONMENT=test
APPLICATION__LOG_LEVEL=DEBUG
