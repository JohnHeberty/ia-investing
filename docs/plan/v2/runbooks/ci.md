# CI/CD

CI/CD é executado via GitHub Actions no push/PR para `main`. O workflow
`.github/workflows/ci.yml` executa lint, format, type check, testes unitários
e verificação de contratos.

## Verificações locais

Consulte `runbooks/local-development.md` para comandos de verificação.
