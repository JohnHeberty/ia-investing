import { StatePanel } from "@/components/domain";

export default function Loading() {
  return (
    <StatePanel title="Carregando snapshot" detail="Preservando filtros, permissões e as_of…" />
  );
}
